export type EpisodeType = {
    guid: string,
    title: string,
    description?: string,
    link?: string,
    pubDate: string,
    duration?: string,
    url: string,
    length?: Number,
    new: boolean
};
